package main

import (
	"fmt"
)

func main() {

	fmt.Println("UPGMA!")

	//read in matrix, convert it to a distance matrix
	//read in species names too
	//run UPGMA on these two things
	//print the resulting tree to Newick format in a file
	//upload that file to the internet
	//look at tree

}
